export interface authRegister {
    email: string;
    password: string;
    role: string;
}

export interface userProfileRegister {
    cedula: string;
    firstName: string;
    lastName: string;
    phone: string;
}

